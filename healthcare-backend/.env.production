NODE_ENV=production
PORT=5000

# MongoDB Atlas Connection String
# Get from: https://cloud.mongodb.com → Your Cluster → Connect → Drivers
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/healthcare-appointments?retryWrites=true&w=majority

# Frontend URL (set to your Vercel frontend URL after deployment)
CORS_ORIGIN=https://your-frontend-vercel-url.vercel.app

# Optional: Add other environment variables as needed
APPOINTMENT_EXPIRY_MINUTES=5
